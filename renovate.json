{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],

  "ignorePaths": ["zmsbase/**"],

  "baseBranchPatterns": [
    "next",
    "/^release\\/.*/"
  ],

  "timezone": "Europe/Berlin",
  "schedule": ["* 0-4 * * *"],

  "constraints": {
    "php": "8.4.*"
  },

  "packageRules": [
    {
      "description": "Surefire 3.6.0 dropped surefire-testng; Cucumber TestNG then discovers 0 scenarios.",
      "matchManagers": ["maven"],
      "matchPackageNames": ["org.apache.maven.plugins:maven-surefire-plugin"],
      "matchFileNames": ["zmsautomation/pom.xml"],
      "allowedVersions": "<3.6.0"
    },
    {
      "description": "In-range npm overrides must bump package.json. A lockfile-only update runs `npm install pkg@version`, which npm rejects with EOVERRIDE when that spec differs from the override.",
      "matchManagers": ["npm"],
      "matchDepTypes": ["overrides"],
      "rangeStrategy": "bump"
    },
    {
      "description": "Composer lockfile updates must be allowed to move locked transitive packages, such as sebastian/diff when PHPUnit is bumped.",
      "matchManagers": ["composer"],
      "postUpdateOptions": ["composerWithAll"]
    }
  ]
}
