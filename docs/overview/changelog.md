---
outline: deep
---

# Changelog

The canonical changelog lives in the repository as [`CHANGELOG.md` on `main`](https://github.com/it-at-m/eappointment/blob/main/CHANGELOG.md).

The section below loads that same file from the [GitHub Contents API](https://docs.github.com/en/rest/repos/contents?apiVersion=2022-11-28#get-repository-content) (`ref=main`) when you open this page, so it stays aligned with the repo without duplicating text in the docs tree.

Further down, the static **Berlin** block preserves the basis-version history from the Berlin lineage. **Version 2.25.00** is the last Berlin-line release recorded there—it is the point at which this repository hard-forked from Berlin; ongoing release notes for this fork are in the Munich changelog above.

<ChangelogEmbed />

<BerlinChangelogEmbed />
