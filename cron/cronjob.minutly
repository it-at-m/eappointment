#!/bin/bash
DIR=$(dirname ${BASH_SOURCE[0]})
BIN=$DIR/../vendor/bin
EXPORT=$DIR/../data/
set -a
[ -e /etc/sysconfig/zms ] && source /etc/sysconfig/zms
set +a

if [ "$ZMS_CRONMASTER" ]; then
    $BIN/deleteSessionData Zmsappointment 30 --commit
fi
