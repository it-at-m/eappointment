#!/usr/bin/env php
<?php
require_once(__DIR__."/script_bootstrap.php");

if (isset($argv[2]) && $argv[2] == '--commit') {
    \BO\Zmsdb\Helper\DayoffDeleteByCron::init($argv[1]);
} else {
    \App::$log->notice('deleteDayoffData usage', [
        'usage' => "Usage: {$argv[0]} timeInterval --commit",
    ]);
}
