{# Fallback template wrapper for roles/permissions admin.
   Some environments may look for "rolePermissionAdmin.twig" at the root.
   Delegate to the page-scoped template used elsewhere. #}
{% extends "page/rolePermissionAdmin.twig" %}

