{
    "type": "object",
    "description": "Represents a variant of a request (1 Präsenz, 2 Telefon, 3 Videoberatung, 4 Großkunde, 5 Kleinkunde, 6 Familie, 7 Einzelperson)",
    "example": {
        "id": 1,
        "name": "Präsenz"
    },
    "required": [
        "id",
        "name"
    ],
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "integer",
            "minimum": 1,
            "description": "Unique ID of the request variant"
        },
        "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 50,
            "description": "Name of the request variant"
        }
    }
}