{
    "type": "object",
    "description": "Represents a variant of a request (1 Präsenz, 2 Telefon, 3 Videoberatung, 4 Einzelperson, 5 Familie, 6 Kleinkunde, 7 Großkunde)",
    "example": {
        "id": 1,
        "name": "Präsenz"
    },
    "required": [
        "id",
        "name"
    ],
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "integer",
            "minimum": 1,
            "description": "Unique ID of the request variant"
        },
        "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 50,
            "description": "Name of the request variant"
        }
    }
}