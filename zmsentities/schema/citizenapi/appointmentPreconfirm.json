{
    "type": ["array", "object"],
    "description": "Provides a message indicating the successful pre-confirmation of an appointment.",
    "properties": {
      "message": {
        "type": "string"
      }
    },
    "required": ["message"]
}
