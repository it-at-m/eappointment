{
    "type": ["array", "object"],
    "description": "Contains a message confirming the successful confirmation of an appointment.",
    "properties": {
      "message": {
        "type": "string"
      }
    },
    "required": ["message"]
}
