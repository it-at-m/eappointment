id: set citizen user example.com email
author: ZMS Development
realm: zms
changes:
  # Keep in sync with 09 for stacks that already created citizen with an older email
  # (localhost / mailinator). RFC 2606 reserved — not a real inbox.
  # TODO: consider a unique @mailinator.com address later if ATAF login tests need
  # to receive real mail (same pattern as zmscitizenview Kontakt fills).
  - updateUser:
      name: citizen
      email: citizen@example.com
      emailVerified: true
