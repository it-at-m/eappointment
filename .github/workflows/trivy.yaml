name: 🛡️ Trivy Security Check

on:
  pull_request:
  workflow_call:

jobs:
  build:
    name: 🛡️ Trivy Security Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Run Trivy Security check on Repository
        uses: it-at-m/lhm_actions/action-templates/actions/action-trivy@7cb754b2056cebda6c36db344434f85be398c493 # v1.0.25